# Example triage output (illustrative)

Repo: `psf/requests`  
Issue: (example structure — replace with a real run)

```json
{
  "issue_number": 1234,
  "suggested_labels": ["bug"],
  "confidence": 0.72,
  "rationale": "Reporter describes unexpected exception with reproduction steps.",
  "draft_reply": "Thanks for the report! Could you share the Requests version and a minimal repro script?",
  "related_files": ["requests/models.py"]
}
```

Decision: `approved` (recorded locally only — MVP does not post to GitHub).
